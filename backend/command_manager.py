Fungsi:
- Store commands dengan unique ID
- Track command status (PENDING → SENT → EXECUTING → SUCCESS/FAILED)
- Persist ke JSON file (atau database)
- Get pending commands per slot
- Update command result
